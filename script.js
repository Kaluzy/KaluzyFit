const menu=document.querySelector('.menu'),nav=document.querySelector('nav');menu?.addEventListener('click',()=>{const o=nav.classList.toggle('open');menu.setAttribute('aria-expanded',o)});nav?.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>nav.classList.remove('open')));const observer=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting)e.target.classList.add('visible')}),{threshold:.12});document.querySelectorAll('.reveal').forEach(e=>observer.observe(e));
const key='kaluzyfit-log-v1';let rows=JSON.parse(localStorage.getItem(key)||'[]');const $=s=>document.querySelector(s),body=$('#log-body');$('#date').value=new Date().toISOString().slice(0,10);
function save(){localStorage.setItem(key,JSON.stringify(rows))}function n(v,d=1){const x=Number(v);return Number.isFinite(x)?x.toFixed(d):'—'}function render(){body.innerHTML='';[...rows].sort((a,b)=>b.date.localeCompare(a.date)).forEach(r=>{const tr=document.createElement('tr');tr.innerHTML=`<td>${r.date}</td><td>${r.weight||'—'}</td><td>${r.waist||'—'}</td><td>${r.steps||'—'}</td><td>${r.workout}</td><td>${r.sleep||'—'}</td><td>${r.adherence?`${r.adherence}%`:'—'}</td><td>${r.notes||''}</td><td><button class="delete-row" data-id="${r.id}">Delete</button></td>`;body.appendChild(tr)});const sorted=[...rows].sort((a,b)=>a.date.localeCompare(b.date)),latest=sorted.at(-1),last=sorted.slice(-7),weights=last.map(r=>Number(r.weight)).filter(Boolean),steps=last.map(r=>Number(r.steps)).filter(Number.isFinite);$('#latest-weight').textContent=latest?.weight?`${n(latest.weight)} lb`:'—';$('#average-weight').textContent=weights.length?`${n(weights.reduce((a,b)=>a+b,0)/weights.length)} lb`:'—';$('#average-steps').textContent=steps.length?Math.round(steps.reduce((a,b)=>a+b,0)/steps.length).toLocaleString():'—';$('#workout-count').textContent=rows.filter(r=>['Yes','Yellow Day','Red Day'].includes(r.workout)).length;document.querySelectorAll('.delete-row').forEach(b=>b.addEventListener('click',()=>{rows=rows.filter(r=>r.id!==b.dataset.id);save();render()}))}
$('#log-form').addEventListener('submit',e=>{e.preventDefault();const r={id:crypto.randomUUID(),date:$('#date').value,weight:$('#weight').value,waist:$('#waist').value,steps:$('#steps').value,workout:$('#workout').value,sleep:$('#sleep').value,adherence:$('#adherence').value,notes:$('#notes').value.trim()};const existing=rows.findIndex(x=>x.date===r.date);if(existing>=0){r.id=rows[existing].id;rows[existing]=r}else rows.push(r);save();render();e.target.reset();$('#date').value=new Date().toISOString().slice(0,10)});$('#export').addEventListener('click',()=>{const head=['Date','Morning Weight','Waist','Steps','Workout','Sleep Hours','Plan Adherence %','Notes'],esc=v=>`"${String(v??'').replaceAll('"','""')}"`,csv=[head,...rows.sort((a,b)=>a.date.localeCompare(b.date)).map(r=>[r.date,r.weight,r.waist,r.steps,r.workout,r.sleep,r.adherence,r.notes])].map(x=>x.map(esc).join(',')).join('\n'),a=document.createElement('a');a.href=URL.createObjectURL(new Blob([csv],{type:'text/csv'}));a.download='KaluzyFit-Tracking-Log.csv';a.click();URL.revokeObjectURL(a.href)});$('#clear').addEventListener('click',()=>{if(confirm('Clear every saved KaluzyFit entry on this device?')){rows=[];save();render()}});render();